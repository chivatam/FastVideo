# SparseFP4 Native Composition Skill

Install:

```text
FastVideo/.agents/skills/sparsefp4-native-composition/
```

This replaces the routing-first experimental framing with the direct native composition test.

Main rule:

**No native D0/P3 = no SparseFP4 composition claim.**

See `INVOKE.txt` for the recommended coding-agent prompt.
